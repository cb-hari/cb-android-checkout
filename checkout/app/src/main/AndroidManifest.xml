<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.chargebee.android.checkout">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity">
        </activity>
        <activity android:name=".UpgradeActivity">

        </activity>
        <activity android:name="com.chargebee.checkout.ChargebeeWebViewActivity"/>
        <activity
            android:name=".TrialSubscriptionActivity"
            android:label="@string/title_activity_trial_subscription"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".PaidSubscriptionActivity"
            android:label="@string/title_activity_paid_subscription"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>